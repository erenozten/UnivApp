@model IEnumerable<UnivApp.Models.OfficeAssignment>

<h2>Office Assignments</h2>
@Html.Partial("/Views/PartialViews/SearchBoxAndCreateLink.cshtml")
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instructor.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instructor.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            <span class="btn-group">
                @Html.ActionLink("Edit", "Edit", new { id = item.InstructorID }, new { @class = "button" })
            </span> <span class="btn-group">
                @Html.ActionLink("Details", "Details", new { id = item.InstructorID }, new { @class = "button" })
            </span> <span class="btn-group">
                @Html.ActionLink("Delete", "Delete", new { id = item.InstructorID }, new { @class = "button" })
            </span>
        </td>
    </tr>
    }

</table>
